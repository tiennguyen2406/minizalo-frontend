import React from 'react';

/**
 * Phiên bản Fallback cho Web của CallModal (Mobile).
 * Trên Web chúng ta sử dụng CallModal riêng biệt trong views/web/.
 */
const CallModal = () => {
    return null;
};

export default CallModal;
